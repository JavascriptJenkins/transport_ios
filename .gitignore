# Xcode
*.xcuserstate
xcuserdata/
DerivedData/
*.xccheckout
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
build/

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods
Pods/
Podfile.lock

# Secrets / connector configs with embedded API keys
*.dxt

# Local secrets injected at build time via xcconfig — never commit
Config.xcconfig

# App Store Connect API credentials — never commit
.env

# macOS
.DS_Store
